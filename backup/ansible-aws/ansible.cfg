[defaults]
host_key_checking = False
inventory = inventory
remote_user = ubuntu
private_key_file = ~/.ssh/votex_key

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
